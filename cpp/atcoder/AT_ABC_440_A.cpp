#include <bits/stdc++.h>
using namespace std;

// 使用 ll 代替 long long，防止数据溢出导致报错
typedef long long ll;

void solve() {
    // --- 在这里编写你的代码逻辑 ---
    
}

int main() {
    int a,b;
    cin>>a>>b;
    int m;
    m=a*pow(2,b);
    printf("%d",m);
    return 0;
}
